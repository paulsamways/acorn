@model ActivateViewModel
@{
  ViewData["Title"] = "Activate";
}

<main class="f-grow-1 f-column f-align-center">
  <form class="w-480 f-column g-3" asp-route="@Routes.AccountActivatePostRoute" asp-route-returnurl="@ViewData[Acorn.Controllers.AccountController.ViewDataReturnUrl]" method="POST">

    <partial name="_ValidationSummary" />

    <input type="hidden" asp-for="Code" />

    <div class="f-column">
      <label asp-for="Email"></label>
      <input asp-for="Email" type="email" readonly required />
    </div>

    <div class="f-column">
      <label asp-for="Password"></label>
      <input asp-for="Password" type="password" autocomplete="new-password" autofocus required aria-validation />
    </div>

    <div class="f-column">
      <label asp-for="ConfirmPassword">Confirm password</label>
      <input asp-for="ConfirmPassword" type="password" autocomplete="new-password" required aria-validation />
    </div>

    <div class="f-column mt-5">
      <button type="submit">Submit</button>
    </div>
  </form>
</main>
